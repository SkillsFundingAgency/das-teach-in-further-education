@using SFA.DAS.TeachInFurtherEducation.Contentful.Model.Interim
@using SFA.DAS.TeachInFurtherEducation.Web.Helpers
@using SFA.DAS.TeachInFurtherEducation.Web.Services
@model ContentBox
@{
    var backColor = StringHelper.GetCssClassName();
    <style nonce="@Context.GetNonce()">
    .@backColor {
        background-color: @Model.BackgroundColor;
    }
    </style>
    
    <div id="@Model.DataId" class="govuk-width-container fe-content-box">
        <div class="govuk-grid-row">
            <div class="govuk-grid-column-two-thirds @backColor content-box"> 
                @ComponentService.ToHtmlString(Model.ContentBoxContents)
            </div>
        </div>
    </div>
}