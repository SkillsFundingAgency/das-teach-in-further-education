@using System.Web;
@using SFA.DAS.TeachInFurtherEducation.Contentful.Model.Interim;
@using SFA.DAS.TeachInFurtherEducation.Web.Services
@model PageContentModel

@if (Model != null && Model.PageTemplate == "Home")
{
@await Html.PartialAsync(

                             "_HomePageTemplate",

                                 @Model

                             )
}
